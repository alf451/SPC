# URL del backend durante lo sviluppo (npm run dev). In produzione (dopo
# "npm run build") il frontend viene servito dallo stesso backend FastAPI
# sulla stessa origine, quindi questa variabile non serve piu' - vedi
# src/api/client.js e docs/installazione.md.
VITE_API_BASE_URL=http://127.0.0.1:8000
